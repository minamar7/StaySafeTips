async function openSOS() {
    if(!isPremium) { nav('premium'); return; }
    try {
        const res = await fetch('./emergency_hub.json');
        const data = await res.json();
        
        // Ξεκινάμε το HTML με έμφαση στον Παγκόσμιο Αριθμό
        let html = `
        <div class="glass-card" style="text-align:left; max-height:80vh; overflow-y:auto; padding:20px; border-color:var(--danger);">
            <h1 style="color:var(--danger); font-size:36px; margin:0 0 10px 0; text-align:center;">🌍 GLOBAL SOS</h1>
            
            <div style="background:rgba(239,68,68,0.15); padding:20px; border-radius:20px; text-align:center; border:2px solid var(--danger); margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; color:var(--text);">${data.global_standard.title}</h2>
                <div style="font-size:50px; font-weight:900; color:var(--text); margin:10px 0;">${data.global_standard.number}</div>
                <p style="margin:0; font-size:12px; color:var(--muted);">${data.global_standard.note}</p>
            </div>

            <hr style="opacity:0.1; margin:20px 0;">`;

        // Δημιουργία λίστας ανά περιοχή
        data.regions.forEach(r => {
            html += `<h3 style="color:var(--accent); font-size:24px; margin:25px 0 15px 10px; border-left:4px solid var(--accent); padding-left:10px;">${r.area}</h3>`;
            
            r.countries.forEach(c => {
                html += `
                <div style="background:rgba(255,255,255,0.03); padding:15px; border-radius:18px; margin-bottom:12px; border:1px solid rgba(255,255,255,0.05);">
                    <div style="font-size:18px; font-weight:bold; color:var(--gold); margin-bottom:8px;">${c.name}</div>
                    <div style="display:flex; justify-content:space-between; font-size:16px; font-weight:800;">
                        <span>🚓 ${c.police}</span>
                        <span>🚑 ${c.medical}</span>
                        <span>🚒 ${c.fire}</span>
                    </div>
                    ${c.universal ? `<div style="font-size:12px; color:var(--muted); margin-top:8px; text-align:right;">Universal: ${c.universal}</div>` : ''}
                </div>`;
            });
        });

        html += `
            <button class="main-cta" style="margin-top:20px; background:var(--danger); color:white;" onclick="nav('home')">ΕΠΙΣΤΡΟΦΗ</button>
        </div>`;

        document.getElementById('screen_quiz').innerHTML = html;
        nav('quiz');
    } catch(e) { 
        alert("Σφάλμα: Βεβαιωθείτε ότι το αρχείο emergency_hub.json υπάρχει στο GitHub σας."); 
    }
}
