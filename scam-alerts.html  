<!DOCTYPE html>
<html lang="el">
<head>
    <meta charset="UTF-8">
    <title>Elite Live Alerts</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
    <style>
        :root { --danger: #ef4444; --bg: #020617; --card: #1e293b; --accent: #38bdf8; --gold: #facc15; }
        body { margin: 0; background: var(--bg); font-family: -apple-system, sans-serif; color: #f8fafc; padding: 20px; }
        
        /* Live Header */
        .header { text-align: center; margin-bottom: 30px; padding-top: 10px; }
        .live-status { display: inline-flex; align-items: center; gap: 8px; background: rgba(239, 68, 68, 0.1); padding: 5px 15px; border-radius: 20px; border: 1px solid var(--danger); margin-bottom: 10px; }
        .dot { width: 8px; height: 8px; background: var(--danger); border-radius: 50%; animation: pulse 1.5s infinite; }
        @keyframes pulse { 0% { opacity: 1; transform: scale(1); } 50% { opacity: 0.4; transform: scale(1.2); } 100% { opacity: 1; transform: scale(1); } }
        
        .back-btn { color: var(--accent); text-decoration: none; font-weight: bold; font-size: 0.9rem; }

        /* Alert Cards */
        #feed { display: grid; gap: 15px; }
        .alert-card { 
            background: var(--card); border-radius: 22px; padding: 20px; 
            border-left: 5px solid var(--danger); box-shadow: 0 10px 25px rgba(0,0,0,0.3);
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn { from { opacity: 0; transform: translateX(-20px); } to { opacity: 1; transform: translateX(0); } }

        .alert-card .meta { font-size: 0.7rem; color: var(--accent); font-weight: 800; text-transform: uppercase; margin-bottom: 8px; display: block; }
        .alert-card h3 { margin: 0 0 10px; font-size: 1.1rem; color: var(--gold); line-height: 1.2; }
        .alert-card p { margin: 0; font-size: 0.9rem; color: #94a3b8; line-height: 1.5; }
        
        .read-btn { display: inline-block; margin-top: 15px; color: white; background: var(--danger); padding: 8px 16px; border-radius: 12px; text-decoration: none; font-size: 0.8rem; font-weight: bold; }

        .shimmer { text-align: center; padding: 40px; color: var(--muted); font-style: italic; }
    </style>
</head>
<body>

    <a href="index.html" class="back-btn">← DASHBOARD</a>

    <header class="header">
        <div class="live-status">
            <span class="dot"></span>
            <span style="color:var(--danger); font-size:0.7rem; font-weight:900; letter-spacing:1px;">LIVE UPDATES</span>
        </div>
        <h1>Scam Alerts</h1>
        <p style="color:var(--accent); font-weight:bold; font-size:0.8rem; margin:0;">ΠΑΓΚΟΣΜΙΑ ΠΑΡΑΚΟΛΟΥΘΗΣΗ ΑΠΑΤΩΝ</p>
    </header>

    <div id="feed">
        <div class="shimmer">Συγχρονισμός με διεθνή κέντρα ασφαλείας...</div>
    </div>

<script>
    // Προστασία Elite
    if(localStorage.getItem('isElite') !== 'true') { window.location.href = 'index.html'; }

    async function getLiveScams() {
        const rssUrl = 'https://www.consumer.ftc.gov/blog/feed';
        const api = `https://api.rss2json.com/v1/api.json?rss_url=${encodeURIComponent(rssUrl)}`;

        try {
            const res = await fetch(api);
            const data = await res.json();
            const container = document.getElementById('feed');
            container.innerHTML = '';

            data.items.slice(0, 8).forEach(item => {
                const div = document.createElement('div');
                div.className = 'alert-card';
                const cleanDesc = item.description.replace(/<[^>]*>?/gm, '').substring(0, 130);
                
                div.innerHTML = `
                    <span class="meta">🔴 NEW THREAT • ${new Date(item.pubDate).toLocaleDateString('el-GR')}</span>
                    <h3>${item.title}</h3>
                    <p>${cleanDesc}...</p>
                    <a href="${item.link}" target="_blank" class="read-btn">ΟΔΗΓΙΕΣ ΠΡΟΣΤΑΣΙΑΣ</a>
                `;
                container.appendChild(div);
            });
        } catch (e) {
            document.getElementById('feed').innerHTML = "<p style='text-align:center;'>Σφάλμα σύνδεσης. Ελέγξτε το δίκτυο.</p>";
        }
    }

    getLiveScams();
</script>

</body>
</html>
