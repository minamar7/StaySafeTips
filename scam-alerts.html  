<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8" />
  <title>Scam Alerts – Stay Safe Elite</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
  <link rel="stylesheet" href="./styles.css" />

  <style>
    :root {
      --danger: #ef4444;
      --warning: #f59e0b;
      --info: #3b82f6;
    }

    .alert-feed {
      padding: 15px;
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .alert-block {
      background: rgba(30, 41, 59, 0.7);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-left: 5px solid var(--info); /* Δυναμικό χρώμα */
      border-radius: 16px;
      padding: 18px;
      animation: slideIn 0.4s ease-out forwards;
      position: relative;
    }

    @keyframes slideIn {
      from { opacity: 0; transform: translateX(20px); }
      to { opacity: 1; transform: translateX(0); }
    }

    .threat-level {
      position: absolute;
      top: 15px;
      right: 15px;
      font-size: 0.65rem;
      font-weight: 900;
      padding: 4px 8px;
      border-radius: 6px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .alert-block h3 {
      margin: 0 0 10px 0;
      color: #fff;
      font-size: 1.1rem;
      max-width: 80%;
    }

    .alert-block ul {
      padding-left: 18px;
      margin: 10px 0;
    }

    .alert-block li {
      color: #cbd5e1;
      font-size: 0.85rem;
      margin-bottom: 6px;
      line-height: 1.3;
    }

    .action-box {
      background: rgba(0, 0, 0, 0.3);
      padding: 12px;
      border-radius: 10px;
      margin-top: 12px;
      border: 1px dashed rgba(255, 255, 255, 0.2);
    }

    .action-box strong {
      color: #facc15;
      font-size: 0.8rem;
      display: block;
      margin-bottom: 4px;
      text-transform: uppercase;
    }

    .action-box p {
      margin: 0;
      color: #fff;
      font-size: 0.85rem;
    }

    #refresh-alerts-btn {
      margin: 20px;
      padding: 16px;
      background: linear-gradient(135deg, #1e293b, #0f172a);
      color: #facc15;
      border: 1px solid #facc15;
      border-radius: 14px;
      font-weight: 800;
      cursor: pointer;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    #refresh-alerts-btn:active {
      transform: scale(0.98);
      background: #facc15;
      color: #000;
    }

    /* Threat Colors */
    .level-high { border-left-color: var(--danger); }
    .level-high .threat-level { background: var(--danger); color: white; }
    
    .level-medium { border-left-color: var(--warning); }
    .level-medium .threat-level { background: var(--warning); color: black; }
  </style>
</head>

<body>
  <div class="app-shell">
    <header class="app-header">
      <div>
        <div style="display: flex; align-items: center; gap: 10px;">
            <h1 style="margin:0;">🛑 Scam Radar</h1>
            <span id="live-indicator" style="color:#22c55e; font-size: 0.7rem; font-weight: bold; animation: pulse 2s infinite;">• LIVE</span>
        </div>
        <p class="sub">Πραγματική παρακολούθηση νέων απειλών</p>
      </div>
    </header>

    <main class="alert-feed" id="alerts-container">
      </main>

    <button id="refresh-alerts-btn">ΣΑΡΩΣΗ ΓΙΑ ΝΕΕΣ ΑΠΕΙΛΕΣ</button>
  </div>

  <script>
    // Premium Gate
    (function () {
      const isPremium = localStorage.getItem("ss_premium") === "true";
      if (!isPremium) {
        window.location.href = "index.html";
        return;
      }
    })();

    const alertsData = [
      {
        title: "SMS Επιβεβαίωσης Τράπεζας",
        level: "high",
        bullets: [
          "Μήνυμα ότι ο λογαριασμός σας θα κλειδωθεί.",
          "Σύνδεσμος (link) προς ψεύτικη σελίδα e-banking.",
          "Χρήση επείγουσας γλώσσας για να σας πιέσει."
        ],
        action: "Μην πατάτε ποτέ συνδέσμους σε SMS. Μπείτε μόνο από το επίσημο app της τράπεζας."
      },
      {
        title: "Απάτη 'Συγγενής σε Ανάγκη'",
        level: "high",
        bullets: [
          "Κλήση από άγνωστο για ατύχημα δικού σας ανθρώπου.",
          "Απαίτηση για άμεση μεταφορά χρημάτων ή παράδοση μετρητών.",
          "Πιθανή χρήση AI για κλωνοποίηση φωνής."
        ],
        action: "Κλείστε το τηλέφωνο και καλέστε εσείς τον συγγενή σας στον γνωστό του αριθμό."
      },
      {
        title: "Ψεύτικο Ειδοποιητήριο ΕΛΤΑ/Courier",
        level: "medium",
        bullets: [
          "Email ή SMS για δέμα που 'εκκρεμεί'.",
          "Ζητάει πληρωμή μικρού ποσού για έξοδα αποστολής.",
          "Σας οδηγεί σε σελίδα υποκλοπής κάρτας."
        ],
        action: "Ελέγξτε τον κωδικό αποστολής απευθείας στο επίσημο site της εταιρείας."
      },
      {
        title: "Επενδυτικές Απάτες (Crypto)",
        level: "medium",
        bullets: [
          "Υποσχέσεις για εγγυημένα κέρδη χωρίς ρίσκο.",
          "Πίεση να επενδύσετε 'τώρα' πριν κλείσει η ευκαιρία.",
          "Εμφάνιση ψεύτικων κερδών σε πλατφόρμες-μαϊμού."
        ],
        action: "Αγνοήστε διαφημίσεις με επώνυμους που 'πλούτισαν ξαφνικά'. Δεν υπάρχει εγγυημένο κέρδος."
      }
    ];

    const container = document.getElementById("alerts-container");
    const refreshBtn = document.getElementById("refresh-alerts-btn");

    function renderAlerts() {
      container.innerHTML = "";
      
      // Shuffle και επιλογή 3 τυχαίων για "φρέσκο" περιεχόμενο
      const shuffled = [...alertsData].sort(() => Math.random() - 0.5).slice(0, 3);

      shuffled.forEach((alert, index) => {
        setTimeout(() => {
          const div = document.createElement("div");
          div.className = `alert-block level-${alert.level}`;

          div.innerHTML = `
            <span class="threat-level">${alert.level === 'high' ? 'CRITICAL' : 'WARNING'}</span>
            <h3>${alert.title}</h3>
            <ul>
              ${alert.bullets.map(b => `<li>${b}</li>`).join('')}
            </ul>
            <div class="action-box">
              <strong>ΟΔΗΓΙΑ ΑΣΦΑΛΕΙΑΣ:</strong>
              <p>${alert.action}</p>
            </div>
          `;

          container.appendChild(div);
          if (window.navigator.vibrate) window.navigator.vibrate(10);
        }, index * 150);
      });
    }

    refreshBtn.addEventListener("click", () => {
      refreshBtn.textContent = "ΣΑΡΩΣΗ...";
      setTimeout(() => {
        renderAlerts();
        refreshBtn.textContent = "ΣΑΡΩΣΗ ΓΙΑ ΝΕΕΣ ΑΠΕΙΛΕΣ";
      }, 800);
    });

    renderAlerts();
  </script>
</body>
</html>
